/*
const grade = Number(prompt("Enter students Grade: "));
if (isNaN(grade) || grade <= 0 || grade >= 100) {
  alert("Please enter a valid score between 0 and 100");
} else {

  switch (true) {
    case grade >= 90:
      console.log(`Your Grade is A ${grade}`);
      break;
    case grade >= 80:
      console.log(`Your Grade is B ${grade}`);
      break;
    case grade >= 70:
      console.log(`Your Grade is C ${grade}`);
      break;
    case grade >= 60:
      console.log(`Your Grade is D ${grade}`);
      break;
    case grade >= 50:
      console.log(`Your Grade is E ${grade}`);
      break;
    default:
        console.log(`Your Grade is F ${grade}`);
    
  }
}

*/
